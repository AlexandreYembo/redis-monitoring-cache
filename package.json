{
  "name": "rediscachemanager",
  "version": "1.0.0",
  "description": "Cache manager for Redis by using queue service (RabbitMQ, Service Bus, etc)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.3",
    "bluebird": "^3.5.5",
    "cluster": "^0.7.7",
    "npm": "^6.10.1",
    "redis": "^2.8.0"
  }
}
